node_modules/
dist/
build/
*.wasm
*.js.map
.vite/
.DS_Store
.env
.env.local
.emscripten_cache/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Built WASM artifacts - regenerated by scripts/build.ps1. Users install
# from npm get the prebuilt version via the package's `files` field;
# contributors rebuild locally.
public/rdm7-sandbox.js
public/rdm7-sandbox.wasm

# User-local IDE/agent config.
.claude/

# Synced firmware sources (pulled via scripts/sync-firmware.ps1)
# Kept out of git to avoid double-ownership with the firmware repo.
firmware_src/
