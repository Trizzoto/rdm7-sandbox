/* default_layout.h — Stub for WASM build (no default layout generation) */
#pragma once
#include "esp_stubs.h"

static inline esp_err_t generate_default_layout(void) { return ESP_OK; }
static inline void generate_rpm_meter_test_layout(void) { }
