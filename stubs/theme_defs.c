/* theme_defs.c — any firmware-side theme constants that are declared
 * as `extern` rather than `#define`. Most of theme.h is preprocessor
 * defines so this will stay small.
 *
 * Empty for now; kept so CMakeLists's STUB_SOURCES doesn't have a
 * dangling reference and the list is easy to extend.
 */
